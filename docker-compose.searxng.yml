version: "3.8"
services:
  searxng:
    image: searxng/searxng:latest
    network_mode: host
    environment:
      SEARXNG_BASE_URL: http://localhost:8888
      SEARXNG_CSRF: "false"
    restart: unless-stopped